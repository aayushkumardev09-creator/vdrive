export function initSentry() {
  // Keep the app simple/reliable: Sentry integration is optional.
  // When Sentry deps are installed, you can wire them up here.
  // Intentionally a no-op to avoid build failures when deps are missing.
  return;
}


